/** Site-wide constants shared across route metadata and structured data. */

export const SITE_URL = 'https://www.hjsconstruction.com';

/** Square brand mark. Used for JSON-LD `logo`/`image`, not for social cards. */
export const LOGO_PATH = '/logo/logo.png';

/** Social share card at the 1200x630 the Open Graph metadata declares. */
export const OG_IMAGE_PATH = '/logo/og-image.png';
export const OG_IMAGE_WIDTH = 1200;
export const OG_IMAGE_HEIGHT = 630;
export const OG_IMAGE_ALT = 'HJS Construction';
